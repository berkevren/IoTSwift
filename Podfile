source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '9.0'
use_frameworks!

target 'Scorch' do
    $awsVersion = '~> 2.8.0'
    pod 'AWSMobileClient', $awsVersion
    pod 'AWSIoT', $awsVersion
end
